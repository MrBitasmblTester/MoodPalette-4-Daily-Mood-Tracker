import React from "react";

export default function CalendarHeatmap({data}) {
  return (
    <div className="heatmap">
      {/* Iterate days and render divs colored by data[date] */}
    </div>
  );
}